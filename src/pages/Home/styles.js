const styles = {
  hero: { margin: (theme) => theme.spacing(2) },
  buttonGroup: {
    display: "flex",
    flexDirection: "column",
    jsutifyContent: "center",
    alignItems: "center",
    gap: "5px",
    pb: "50px",
  },
};

export default styles;
